# Optional environment overrides for beetle.service
# Keep the state root explicit so files live outside the release directories.
BEETLE_STATE_ROOT=/var/lib/beetle

# Example: enable the local config HTTP server on Linux.
# BEETLE_CONFIG_HTTP_LISTEN=127.0.0.1:8080
