/** 导航栏与页面主体内容最大宽度（px），与 theme breakpoints.lg 一致 */
export const CONTENT_MAX_WIDTH = 1200

/** 顶栏与侧栏品牌行统一最小高度（px），保证水平分区对齐 */
export const TOP_BAR_MIN_HEIGHT = 56

/** 设置抽屉宽度（px） */
export const SETTINGS_DRAWER_WIDTH = 360

/** 侧栏宽度（px） */
export const SIDEBAR_WIDTH_EXPANDED = 240

/**
 * 小屏抽屉内侧栏纸面宽度（px），略宽于 `SIDEBAR_WIDTH_EXPANDED` 以容纳触控与长标签。
 */
export const SIDEBAR_DRAWER_WIDTH = 280

/** 小屏断点：低于此宽度侧栏收起到抽屉 */
export const SIDEBAR_DRAWER_BREAKPOINT = 900
