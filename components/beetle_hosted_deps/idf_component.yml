dependencies:
  espressif/esp_wifi_remote:
    version: "1.2.4"
    require: private
    rules:
      - if: target in [esp32p4]
  espressif/esp_hosted:
    version: "2.12.3"
    require: private
    rules:
      - if: target in [esp32p4]
