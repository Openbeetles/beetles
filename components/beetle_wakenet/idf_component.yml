dependencies:
  espressif/cjson:
    version: "1.7.19"
    rules:
      - if: "target in [esp32s3]"
  espressif/esp-dsp:
    version: "1.7.0"
    rules:
      - if: "target in [esp32s3]"
  espressif/esp-sr:
    version: "2.4.0"
    rules:
      - if: "target in [esp32s3]"
